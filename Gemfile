source "https://rubygems.org"

# gem "rails"
gem "jekyll", "~> 4.4"
gem "webrick"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
# gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw, :jruby]

# Ruby 3.4+/4 extracts some stdlib components into bundled gems.
# Keep these explicit for smooth local installs across Ruby versions.
gem "base64"
gem "bigdecimal"
gem "csv", "~> 3.3"
gem "logger"
gem "mutex_m"
